import Head from 'next/Head';
import { Meta } from 'components/layout';

export default function Home() {
  return(
    <main>
      <Head>
        <title>Landing boilerplate - Create landing faster</title>
        <Meta />
      </Head>
    </main>
  );
}